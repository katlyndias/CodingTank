import java.util.Scanner;

//public class NumeroParEPositivo {

    //public static void main(String[] args) {
       // Scanner ler = new Scanner(System.in);
       // String ehPos = ("O número %s é positivo");
      //  String ehNeg = ("O número %s é negativo");
       // String ehZero = ("O número %s é zero e par");
       // String ehPar = ("O número %s é par");
       // String ehImpar = ("O número %s é ímpar");
       // System.out.printf("Informe um número: \n");
      //  int numero = ler.nextInt();
      //  boolean ehPositivo = numero > 0;
      //  boolean ehNegativo = !ehPositivo;
      //  boolean ePar = numero % 2 == 0;
      //  boolean eImpar = !ehPar;

      //  if (numero == 0) {
       //     System.out.println(ehZero);
      //  }
      //  if (ehPositivo && ePar){
      //      System.out.printf(ehPos.formatted(numero) + ehPar);
      //  }

        // ele parou o codigo acima porque estava confuso e refez assim:



//    }
//}
